import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.coderstack.gymmatrix',
  appName: 'gymmatrix-frontend',
  webDir: 'dist'
};

export default config;
